{
  "$schema": "../../../scripts/lib/gallery-score-schema.json",
  "iteration": "v28",
  "render": "eval-pose-30-15.png",
  "reference": "../../../eval/tasks/eyewear-wayfarer-front/reference.jpg",
  "pose": "30,15",
  "evalPoseUsedByHarness": "30,15",
  "thresholds": {
    "silhouetteIoU": 0.45,
    "composite": 0.30
  },
  "perGate": {
    "silhouetteIoU": 0.7023528687767954,
    "ssim": 0.18920611648871807,
    "perceptualHash": 0.453125
  },
  "composite": 0.4985632693350131,
  "gateStatus": {
    "silhouetteIoU >= 0.45": true,
    "composite >= 0.30": true
  },
  "diagnostics": [
    {
      "code": "image.silhouette.partial",
      "severity": "warning",
      "message": "Silhouette IoU 0.702 below 0.8 — outline matches the reference category but mismatches in proportion or corner shape.",
      "hint": "Largest silhouette mismatch in the lower-left quadrant — focus geometry edits there. (XOR pixel counts: UL=1465, UR=3010, LL=7446, LR=2589)."
    },
    {
      "code": "image.structure.mismatch",
      "severity": "warning",
      "message": "SSIM 0.189 below 0.6 — interior structure (lens placement, surface features) does not match.",
      "hint": "Check whether features called out in the reference (camera position, LED, lens insert, surface texture) are present in the render at the correct position."
    },
    {
      "code": "image.layout.mismatch",
      "severity": "info",
      "message": "Perceptual hash similarity 0.453 below 0.7 — coarse layout differs from the reference.",
      "hint": "The render and reference are placed differently in the frame (cropping, scale, or rotation). Re-check render viewport and model centering."
    }
  ],
  "history": {
    "v24.iso": { "composite": 0.2506, "silhouetteIoU": 0.2759, "pose": "iso" },
    "v28.iso": { "composite": 0.3257, "silhouetteIoU": 0.4375, "pose": "iso" },
    "v28.pose30-15": { "composite": 0.4986, "silhouetteIoU": 0.7023, "pose": "30,15" },
    "trend": "iso-pose scoring was misleading — the silhouette at iso (~45/35) is genuinely a worse match than at the eval-task reference pose (30,15). At the harness-canonical pose, silhouetteIoU climbed to 0.70 and composite to 0.50 — well above the 0.45 / 0.30 gates. The agent iterated four times against the wrong pose because nobody had rendered + scored at the proper pose before this commit."
  },
  "note": "score generated by `kernelcad render ... --pose 30,15` + `scripts/scoreRenderVsReference.ts` against the eval task's reference photo. SSIM (0.19) is still well below 0.6 — interior structure (lens placement, surface features, glossiness) doesn't match the reference. Silhouette and composite gates pass but the photo-similarity score is still telling us the model reads as 'glasses-shaped object' more than as 'Ray-Ban Meta'. Future iterations should focus on SSIM (camera bump position, lens insert opacity, body acetate thickness) rather than silhouette."
}
