{
  "$schema": "../../../scripts/lib/gallery-score-schema.json",
  "iteration": "slice-a-task-16",
  "render": "eval-pose-30-15.png",
  "reference": "../../../eval/tasks/eyewear-wayfarer-front/reference.jpg",
  "pose": "30,15",
  "evalPoseUsedByHarness": "30,15",
  "kcadVersion": "0.8.0",
  "thresholds": {
    "silhouetteIoU": 0.45,
    "composite": 0.30,
    "ssim": 0.35
  },
  "perGate": {
    "silhouetteIoU": 0.5102095592030557,
    "ssim": 0.1358592984706893,
    "perceptualHash": 0.390625
  },
  "composite": 0.3739875691427346,
  "gateStatus": {
    "silhouetteIoU >= 0.45": true,
    "composite >= 0.30": true,
    "ssim >= 0.35": false
  },
  "diagnostics": [
    {
      "code": "image.silhouette.partial",
      "severity": "warning",
      "message": "Silhouette IoU 0.510 below 0.8 — outline matches the reference category but mismatches in proportion or corner shape.",
      "hint": "Largest silhouette mismatch in the lower-left quadrant — focus geometry edits there. (XOR pixel counts: UL=2805, UR=2838, LL=10548, LR=7532)."
    },
    {
      "code": "image.structure.mismatch",
      "severity": "warning",
      "message": "SSIM 0.136 below 0.6 — interior structure (lens placement, surface features) does not match.",
      "hint": "Check whether features called out in the reference (camera position, LED, lens insert, surface texture) are present in the render at the correct position."
    },
    {
      "code": "image.layout.mismatch",
      "severity": "info",
      "message": "Perceptual hash similarity 0.391 below 0.7 — coarse layout differs from the reference.",
      "hint": "The render and reference are placed differently in the frame (cropping, scale, or rotation). Re-check render viewport and model centering."
    }
  ],
  "history": {
    "v24.iso": { "composite": 0.2506, "silhouetteIoU": 0.2759, "pose": "iso" },
    "v28.iso": { "composite": 0.3257, "silhouetteIoU": 0.4375, "pose": "iso" },
    "v28.pose30-15": { "composite": 0.4986, "silhouetteIoU": 0.7023, "pose": "30,15", "ssim": 0.1892, "note": "previous CLI pose impl (since removed); not reproducible under current pipeline" },
    "slice-a-task-16.pose30-15": { "composite": 0.3740, "silhouetteIoU": 0.5102, "ssim": 0.1359, "pose": "30,15", "note": "Slice A rewrite using referenceImage + variable chamfer + variable fillet + .mirror('yz') + PBR material; --pose plumbing re-added through CLI / headlessRender / DemoPlayer." }
  },
  "note": "Slice A Task 16 rewrite. The solution-expert now reaches for the variable-fillet/chamfer + PBR + mirror + referenceImage primitives. The kcad evaluate step is clean (20 features, OK). Image-similarity numbers above are produced by `kernelcad render --pose 30,15 --hide-reference-images` + scoreRenderVsReference.ts. silhouetteIoU and composite both clear the gallery gates (0.45 / 0.30). SSIM 0.136 is still well below the Slice A target of 0.35 — the gap is structural: the reference photo includes temples + a ground plane + significant table-and-shadow detail the front-face-only model does not reproduce, and the current OCCT BRepMesh facet rate gives flat-looking specular highlights vs. the smooth specular in the photo. Next slice should add: (1) temple geometry (so the side silhouette matches), (2) NURBS curve for the Wayfarer brow (so corner radii match the photo at sub-mm precision), and (3) higher-resolution surface tessellation for smoother specular highlights."
}
