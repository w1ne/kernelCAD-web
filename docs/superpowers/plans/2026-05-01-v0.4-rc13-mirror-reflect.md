# v0.4-rc.13 — `mirror` + `reflect` + bundled rc.12 review fixes — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development. Steps use checkbox (`- [ ]`) syntax. **The spec at `docs/superpowers/specs/2026-05-01-v0.4-rc13-mirror-reflect-design.md` is the source of truth for API shapes, behavior, diagnostic codes, and test coverage matrix.** Each task points back to specific spec components for the detail.

**Goal:** Add `Shape.mirror` / `Shape.reflect` / `Sketch.reflect` to the agent-facing API; close 6 Important + 1 Nit from the rc.12 review.

**Architecture:** Bundled feature + quality milestone (rc.10/rc.11 cadence). Tasks land in dependency order: feature surface first (capture → lowerer → backend), then tests, then review-fix block, then finishing.

**Tech Stack:** TypeScript 5.9, Replicad 0.20.5, vitest 4. No new dependencies.

---

## Task index — 14 tasks

Each task lists files-to-touch and the spec component(s) it implements. Subagents receive the relevant spec section verbatim as context.

### Feature side (Tasks 1–6)

- [ ] **Task 1 — `Shape.reflect(plane)` transform.** Implements spec Component 1.
  - `src/intent/types.ts` — extend `TransformEntry` discriminated union with `{ kind: 'reflect'; plane: PlaneSpec }` and `PlaneSpec` type.
  - `src/capture/proxy.ts` — add `reflect(plane)` method on Shape proxy.
  - `src/capture/captureSession.ts` — register transform via existing helper (match translate/rotate pattern).
  - `src/backends/occt/occtBackend.ts` — add `reflect(plane: PlaneSpec)` instance method calling Replicad's `shape.mirror(plane)` (verify Replicad's mirror is pure-reflection per spec open question Q1).
  - Wherever transforms are applied during lowering — add `'reflect'` case.
  - Tests: `tests/unit/backends/occt/reflect.test.ts` (volume preserved on reflection) + `tests/unit/capture/sketch.test.ts` extended (capture-side test that reflect produces a transform entry, not a feature).
  - **TDD shape:** failing unit test → minimal Replicad call → passing → integration coverage.
  - Commit: `feat(capture): add Shape.reflect(plane) transform`.

- [ ] **Task 2 — `Shape.mirror(plane)` feature.** Implements spec Component 2.
  - `src/intent/types.ts` — add `'mirror'` to `FeatureKind`.
  - `src/capture/proxy.ts` — add `mirror(plane)` method (creates feature, not transform).
  - `src/capture/captureSession.ts` — register mirror feature with `inputs.base` + `metadata.plane`.
  - `src/backends/occt/occtBackend.ts` — add `mirror(plane: PlaneSpec)` method that does `this.union(this.reflect(plane))` internally (per spec Component 2 decision).
  - `src/backends/occt/occtLowerer.ts` — add `case 'mirror':` to switch.
  - Tests: `tests/unit/backends/occt/mirror.test.ts` (mirrored translated box → 2× volume; mirror at origin → fail with hint) + capture test.
  - Commit: `feat(capture): add Shape.mirror(plane) feature for symmetric parts`.

- [ ] **Task 3 — `Sketch.reflect(axis)`.** Implements spec Component 3.
  - `src/capture/sketch.ts` — add `reflect(axis: AxisSpec)` method on Sketch class. Walks the path command list; reflects each `(x, y)` per axis spec; flips arc `clockwise` flag.
  - `src/intent/types.ts` — `AxisSpec` type if not already shared.
  - Tests: `tests/unit/capture/sketch.test.ts` — extend with reflect cases (axis, axis+offset, arcs flipped, labels preserved).
  - Commit: `feat(capture): add Sketch.reflect(axis) for 2D path reflection`.

- [ ] **Task 4 — Diagnostic codes + hints.** Implements spec Component 4.
  - `src/mcp/tools/whyDidThisFail.ts` — add 4 entries to HINTS table (verbatim text in spec Component 4):
    - `feature.mirror.no-base`
    - `feature.mirror.invalid-plane`
    - `feature.mirror.failed`
    - `feature.sketch.reflect.invalid-axis`
  - All `reachable: 'engine-path'`. Confirm sentinel test still passes (after Task 8 it'll be runtime-import-based; before, it's the source-text version with field-ordering convention).
  - Tests: `tests/unit/mcp/whyDidThisFailReachability.test.ts` runs unchanged.
  - Commit: `feat(mcp): add mirror+sketch-reflect diagnostic hints`.

- [ ] **Task 5 — e2e fixture.** Implements spec Component 5.
  - `tests/e2e/fixtures/symmetric-bracket.kcad.ts` — verbatim from spec Component 5.
  - `tests/e2e/cli-acceptance.test.ts` — register the new fixture (mirror the `bracket-bevels` registration pattern from rc.12 Task 5).
  - Tests: `npm test -- tests/e2e/cli-acceptance.test.ts` includes the new fixture and passes.
  - Commit: `test(e2e): add symmetric-bracket fixture demonstrating Shape.mirror`.

- [ ] **Task 6 — Integration tests.** Implements spec Component 6.
  - `tests/integration/backends/occt/reflectTransform.test.ts` (NEW) — reflect-as-transform composition with translate/rotate; canonical face refs unresolvable after reflect; volume preserved.
  - `tests/integration/backends/occt/mirrorFeature.test.ts` (NEW) — mirrored translated box has 2× volume; mirror at origin fails with `feature.mirror.failed` + hint; canonical face refs unresolvable on mirror result.
  - **Coverage matrix from spec Component 6 is the test list — implement each row.**
  - Commit: `test(integration): cover Shape.reflect transform composition + Shape.mirror feature geometry`.

### Quality block — closes rc.12 review (Tasks 7–13)

- [ ] **Task 7 — I1: synth-record discriminated union + N3 + N4 fold-in.** Implements spec Component 7 / I1.
  - `src/backends/occt/occtLowerer.ts` — `applyVariableEdgeFeature` synth-record builder around lines 90–104. Replace silent-drop conditional spreads with a kind switch. Tighten `(feature.inputs.base as { id: string }).id` to a runtime-checked discriminated narrowing. Convert `ApplyVariableEdgeFeatureResult` to a discriminated union (`{ ok: true; shape; diagnostics } | { ok: false; diagnostics }`) to remove `?.shape!` non-null asserts at callers.
  - Add a typed diagnostic `feature.fillet.invalid-edge-ref` / `feature.chamfer.invalid-edge-ref` for unexpected ref kinds.
  - Tests: `tests/unit/backends/occt/variableFillet.test.ts` extended with one negative case (synthetic feature with a `kind: 'feature'` ref in `edge_group_0` should produce the new diagnostic, not a generic `pickEdges` failure).
  - Commit: `refactor(lowerer): tighten applyVariableEdgeFeature ref-kind handling`.

- [ ] **Task 8 — I2: runtime-import reachability sentinel.** Implements spec Component 7 / I2.
  - `tests/unit/mcp/whyDidThisFailReachability.test.ts` — replace source-text `tail.indexOf('}')` parser with a runtime import: `import { HINTS, type HintReachability } from '../../../src/mcp/tools/whyDidThisFail'` (export both from the source file). Iterate `Object.entries(HINTS)`, assert each entry has `reachable` defined and one of the three enum values. Assert the two known direct-lowerer-only codes are correctly classified.
  - `src/mcp/tools/whyDidThisFail.ts` — export `HINTS` (currently file-private) and `HintReachability` so the test can import them. No other behavior change.
  - Removes the field-ordering convention; entries can be in any order. Optionally re-order existing entries to put `hint:` first (for readability). Plan-time judgment.
  - Commit: `refactor(test): runtime-import reachability sentinel removes field-ordering trap`.

- [ ] **Task 9 — I4: banner self-decoupling alias.** Implements spec Component 7 / I4.
  - `package.json:16` — change banner from `const require=createRequire(import.meta.url);` to:
    ```
    import{createRequire as __bcr}from'node:module';
    const require=__bcr(import.meta.url);
    ```
  - The `__bcr` alias dodges the duplicate-binding crash because the source-level import in `server.ts` uses the un-aliased `createRequire`. Bundle is now self-contained.
  - Verify: `rm -f dist/cli/index.js && npm run build:cli && npm test -- tests/integration/cli-bundle/startup.test.ts` — sentinel must pass; bundle must boot.
  - Commit: `fix(build): self-contained banner via __bcr alias removes implicit source coupling`.

- [ ] **Task 10 — I3 + I5 + I6: CHANGELOG + sweep + spawn.test.ts comment.** Implements spec Component 7 / I3, I5, I6.
  - `CHANGELOG.md` — rewrite the rc.12 banner-fix line per spec text (describe what shipped, not the plan's proposal).
  - `CHANGELOG.md:420, 769, 824` (line numbers may have shifted post-rc.12 entries — match by content) — rewrite each remaining hit to native framing. Capture lineage in `~/.claude/projects/-home-andrii/memory/kernelcad_design_lineage.md` BEFORE stripping (per the new "Step 0" rule documented in `feedback_no_competitor_refs_in_repo.md`).
  - `tests/integration/mcp/spawn.test.ts:79-83` — rewrite the skip-rationale comment to reflect rc.12 invariant (qc runs build:cli before tests; the skip exists for inner-loop `npm test` runs without a build).
  - Re-grep with broader regex covering `forgecad|fusion[ ]?360|cadquery|onshape|catia|nx siemens` (case-insensitive). Must return zero hits.
  - Commit: `docs+chore: complete competitor-ref sweep + fix rc.12 CHANGELOG accuracy`.

- [ ] **Task 11 — N1: SKILL.md tool count + drift sentinel.** Implements spec Component 7 / N1.
  - `src/skill/SKILL.md` — update "6 tools" to "13 tools" (verify against `src/mcp/server.ts` TOOLS array). Either explicitly list the 13 or word the surrounding text so the count comes from a single source.
  - `tests/unit/skill/skillToolCountDrift.test.ts` (NEW) — import `TOOLS` from `src/mcp/server.ts`, parse SKILL.md as text, assert the documented number matches `TOOLS.length`. Mirrors the rc.9 list_api drift sentinel.
  - Commit: `test(skill): add tool-count drift sentinel + correct SKILL.md count`.

- [ ] **Task 12 — Component 8: lineage memory entry.** Implements spec Component 8.
  - `~/.claude/projects/-home-andrii/memory/kernelcad_design_lineage.md` — add rc.13 row(s):
    - `Shape.mirror` (union-based) — modeled after Fusion 360's Mirror feature.
    - `Shape.reflect` (pure reflection) — modeled after OpenSCAD's `mirror()` (same word, transform semantics).
    - The two-method split — novel kernelCAD; most CAD systems ship a single verb.
    - Plane spec with offset — native kernelCAD design.
  - This is the FIRST artifact updated for the milestone (per the new Step 0 rule), but in the task order it can come anywhere as long as it lands BEFORE any committed CHANGELOG/spec edit referencing the lineage. The spec is already committed; the rc.13 CHANGELOG entry happens in Task 14.
  - No commit (memory lives outside the repo).

### Finishing (Tasks 13–14)

- [ ] **Task 13 — Pre-finishing audit.** Run before Task 14:
  - `npm run qc` — must pass clean.
  - Manual bundle live-verify: `printf ... | node dist/cli/index.js mcp` returns `serverInfo.version: "0.13.0-rc.12"` (next task bumps to rc.13).
  - Re-grep for competitor refs — must be CLEAN.
  - Spot-check the new mirror/reflect surface with a short script invocation (e.g. `box(10,10,10).translate(5,0,0).mirror({plane: 'yz'})` evaluates and produces non-zero volume).
  - If any check fails, STOP and diagnose before proceeding to Task 14.

- [ ] **Task 14 — Version bump + CHANGELOG + tag.** Implements spec finishing.
  - `package.json` — version `0.13.0-rc.12` → `0.13.0-rc.13`.
  - `CHANGELOG.md` — add `## v0.13.0-rc.13` entry summarizing both feature side (mirror/reflect) and quality block (rc.12 review closure). Native-framing only; no competitor refs.
  - `npm run qc:full` — must pass (Playwright env failure is acceptable per the rc.12 precedent; everything else green).
  - Manual bundle live-verify: `serverInfo.version` reads `"0.13.0-rc.13"`.
  - Commit: `chore(release): v0.13.0-rc.13 mirror+reflect + rc.12 review closure`.
  - Push to `feat/v0.4-rc13-mirror-reflect-and-rc12-fixes` branch (matches naming convention from rc.10/rc.11/rc.12 PRs).
  - Open PR via `gh pr create`, wait for qc CI, squash-merge, tag `v0.13.0-rc.13`, push tag, final live-verify against tag commit.

---

## Subagent dispatch protocol

Per `superpowers:subagent-driven-development`, each task gets:

1. **Scene-setting context.** What rc.13 is, what's already landed (this task's predecessors), what `develop` looks like.
2. **The spec excerpt.** The relevant Component N from `docs/superpowers/specs/2026-05-01-v0.4-rc13-mirror-reflect-design.md`, copied verbatim into the prompt. Subagent does not need to read the spec file.
3. **Discipline rules.** No competitor refs in committed files (capture lineage in memory FIRST). No Claude/AI references in commit messages. "Actually use what you ship" — manual bundle verification at task boundaries where bundle correctness matters.
4. **Status report format.** DONE / DONE_WITH_CONCERNS / NEEDS_CONTEXT / BLOCKED.

After each task: quick diff verification by the controller, then dispatch next.

---

## Open questions resolved at the top of this plan

(All from spec Open Questions section.)

1. **Replicad's `shape.mirror(plane)` semantics.** Resolved at task time by Task 1's TDD setup — first unit test exercises Replicad's mirror directly and asserts it's pure reflection. If it isn't, BLOCK and report.

2. **Plane offset implementation.** Use Replicad's `mirror({ origin, normal })` form if it accepts that shape; otherwise build the reflection matrix manually. Task 1's unit test verifies correctness with a translate-then-reflect example.

3. **`mirror` on a shape touching the plane.** Task 6's `mirrorFeature.test.ts` exercises the degenerate case explicitly. Asserts `feature.mirror.failed` fires with the hint guiding agents to translate or offset.

---

## Self-review

**Spec coverage:** Walked the spec's 8 components — each maps to a task or task group. Component 1 → Task 1. Component 2 → Task 2. Component 3 → Task 3. Component 4 → Task 4. Component 5 → Task 5. Component 6 → Task 6. Component 7 → Tasks 7–11. Component 8 → Task 12. Plus Tasks 13–14 finishing. No gaps.

**Type consistency:** `PlaneSpec` and `AxisSpec` are defined in Task 1 and Task 3 (referenced from `src/intent/types.ts`). `TransformEntry` discriminated union extension is Task 1. `FeatureKind` extension is Task 2. `applyVariableEdgeFeature` and `ApplyVariableEdgeFeatureResult` are referenced in Task 7 (modifying rc.12's existing helper).

**Placeholder scan:** No "TBD", "TODO", "implement later". The TDD code blocks live in the spec (which the dispatcher passes verbatim to subagents). No "see spec for details" without specifying which section.

**Granularity:** 14 tasks, average ~10–25 lines of plan text each (because the spec carries the detail). Comparable scope to rc.10's loft milestone (~14 tasks) and rc.11's variable-radius milestone (~10 tasks plus hotfix).
