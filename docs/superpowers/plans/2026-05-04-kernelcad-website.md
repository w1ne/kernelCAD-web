# kernelcad.com Website + Brand — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Stand up `kernelcad.com` with a single-page landing + auto-updating demo + brand assets (avatar, X header, LinkedIn cover, GitHub social preview) and upload those assets to @KernelCAD on X, the kernelCAD LinkedIn company page, and the GitHub repo. Land all of it today (Day 1 of build-in-public).

**Architecture:** Plain HTML+CSS landing in `site/` (subfolder of `kernelCAD-web`), plain Node scripts for build steps (no framework, no bundler). Brand PNGs generated by Playwright headless screenshots of HTML templates at exact viewport sizes (Playwright reused — already a devDep). Two Cloudflare Pages projects (marketing + visual-debugger app), one Namecheap domain with two CNAMEs. Asset upload to social platforms via chrome-devtools MCP with the user logged in.

**Tech Stack:** HTML5, CSS3, vanilla JS (no bundler). Node 22 for build scripts (already required by repo). Playwright (already devDep) for brand rendering. Cloudflare Pages for hosting. Namecheap for DNS. chrome-devtools MCP for upload automation.

**Spec lineage:** Implements `docs/superpowers/specs/2026-05-04-kernelcad-website-design.md` (commit `85afe76`).

---

## File Structure

All paths relative to `kernelCAD-web/` repo root.

**Create:**

- `site/index.html` — landing page markup
- `site/style.css` — all landing page styles (palette tokens, typography, sections)
- `site/public/favicon.svg` — K-mark favicon
- `site/public/og-image.png` — social share preview (generated by render-brand.mjs)
- `site/scripts/build-demo.mjs` — picks highest-semver demo from `docs/demos/v*/`, copies MP4 + writes demo.json
- `site/scripts/build-demo.test.mjs` — unit tests for build-demo
- `site/scripts/render-brand.mjs` — Playwright script that screenshots brand templates → PNGs
- `site/brand-templates/avatar.html` — 400×400 avatar template (light variant)
- `site/brand-templates/avatar-dark.html` — 400×400 avatar template (dark variant)
- `site/brand-templates/x-header.html` — 1500×500 X header template
- `site/brand-templates/linkedin-cover.html` — 1128×191 LinkedIn cover template
- `site/brand-templates/og-image.html` — 1280×640 OpenGraph / GitHub social preview template
- `site/brand-templates/_shared.css` — shared palette + grid styles for all templates
- `site/README.md` — site dev/deploy notes

**Modify:**

- `package.json` — add scripts: `site:dev`, `site:build`, `site:render-brand`, `site:test`

Each file has one clear responsibility:
- `site/index.html` + `site/style.css`: the landing page only
- `site/scripts/build-demo.mjs`: discover and copy the latest demo MP4
- `site/scripts/render-brand.mjs`: render PNG brand assets from HTML templates
- `site/brand-templates/*.html`: source-of-truth for brand assets (HTML so they're editable as design files)
- `site/brand-templates/_shared.css`: palette tokens shared across all templates (DRY)

---

## Task 1: Scaffold site/ directory and root files

**Files:**
- Create: `site/README.md`, `site/.gitkeep` placeholders for empty subdirs

- [ ] **Step 1: Create directory structure**

```bash
cd ~/projects/kernelCAD-web
mkdir -p site/scripts site/public site/brand-templates
```

- [ ] **Step 2: Create site/README.md**

Write `site/README.md`:

```markdown
# kernelcad.com

Single-page marketing site for kernelCAD. Plain HTML + CSS, zero framework.

## Develop

```bash
cd site && python3 -m http.server 8000
# open http://localhost:8000
```

## Build (run before deploy)

```bash
node site/scripts/build-demo.mjs       # pulls latest demo MP4 from ../docs/demos/v*/
node site/scripts/render-brand.mjs     # renders PNG brand assets
```

## Deploy

Cloudflare Pages, build dir = `site/`, build command = `node site/scripts/build-demo.mjs && node site/scripts/render-brand.mjs`.

Spec: `../docs/superpowers/specs/2026-05-04-kernelcad-website-design.md`
```

- [ ] **Step 3: Commit**

```bash
git add site/README.md
git commit -m "feat(website): scaffold site/ directory"
```

Expected output: 1 file changed, ~22 insertions.

---

## Task 2: Landing page HTML structure

**Files:**
- Create: `site/index.html`

- [ ] **Step 1: Write `site/index.html`**

```html
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <meta name="description" content="kernelCAD — a scriptable, headless-first CAD platform for AI agents. OpenCASCADE kernel. MCP server. Open source." />
  <meta property="og:title" content="kernelCAD — CAD for agents" />
  <meta property="og:description" content="A scriptable, headless-first CAD platform powered by the OpenCASCADE kernel." />
  <meta property="og:image" content="https://kernelcad.com/og-image.png" />
  <meta property="og:url" content="https://kernelcad.com" />
  <meta name="twitter:card" content="summary_large_image" />
  <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
  <link rel="preconnect" href="https://fonts.googleapis.com" />
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
  <link href="https://fonts.googleapis.com/css2?family=Source+Serif+4:ital,wght@0,500;1,500&family=Inter:wght@400;500&family=JetBrains+Mono:wght@400;500&display=swap" rel="stylesheet" />
  <link rel="stylesheet" href="/style.css" />
  <title>kernelCAD — CAD for agents</title>
</head>
<body>
  <div class="page-wrap">

    <nav class="nav">
      <a class="nav-mark" href="/">
        <svg class="k" viewBox="0 0 84 84" fill="none" aria-label="kernelCAD">
          <path d="M 14,12 L 26,12 L 26,34 Q 26,36 27.5,34.5 L 46,12 L 60,12 L 36,40 Q 35,42 36,44 L 60,72 L 46,72 L 27.5,49.5 Q 26,48 26,50 L 26,72 L 14,72 Z" fill="currentColor"/>
        </svg>
        <span>kernel<span class="cad">CAD</span></span>
      </a>
      <div class="nav-links">
        <a class="app-link" href="https://app.kernelcad.com">app ↗</a>
        <a href="https://github.com/w1ne/kernelCAD-web">github</a>
        <a href="https://www.npmjs.com/package/kernelcad">npm</a>
        <a href="https://x.com/kernelcad">@kernelcad</a>
      </div>
    </nav>

    <header class="hero">
      <h1 class="headline">CAD for <span class="accent">agents</span>.</h1>
      <p class="subhead">A scriptable, headless-first CAD platform powered by the OpenCASCADE kernel.</p>
      <div class="modes">
        <span class="mode">browser</span><span class="sep">·</span>
        <span class="mode">terminal</span><span class="sep">·</span>
        <span class="mode">MCP</span><span class="sep">·</span>
        <span class="mode">open source</span>
      </div>
      <div class="ctas-primary">
        <a class="btn btn-primary" href="https://app.kernelcad.com">Try in browser →</a>
        <a class="btn btn-secondary" href="https://github.com/w1ne/kernelCAD-web">★ Star on GitHub</a>
      </div>
      <div class="ctas-tertiary">
        <a href="https://x.com/kernelcad">Follow on X</a>
        <span class="sep">·</span>
        <a href="https://www.linkedin.com/company/kernelcad">Follow on LinkedIn</a>
      </div>
      <button class="install" id="install-btn" aria-label="Copy install command">
        <span><span class="prompt">$</span> npm install -g kernelcad</span>
        <span class="copy">copy</span>
      </button>
    </header>

    <section class="demo-block">
      <div class="demo-meta">
        <span><span class="dot"></span><span id="demo-version">v0.21</span> · live build</span>
        <span>prompt → code → 3D · auto-rotating</span>
      </div>
      <div class="demo-frame">
        <video id="demo-video" autoplay muted loop playsinline preload="metadata"></video>
      </div>
    </section>

    <section class="code-block">
      <div class="code-label">the agent writes this</div>
      <pre class="code"><span class="com">// bracket.kcad.ts</span>
<span class="kw">const</span> w = <span class="fn">param</span>(<span class="str">'Width'</span>, <span class="num">60</span>, { unit: <span class="str">'mm'</span> });
<span class="kw">const</span> h = <span class="fn">param</span>(<span class="str">'Height'</span>, <span class="num">40</span>, { unit: <span class="str">'mm'</span> });
<span class="kw">const</span> t = <span class="fn">param</span>(<span class="str">'Thickness'</span>, <span class="num">5</span>, { unit: <span class="str">'mm'</span> });

<span class="kw">const</span> base = <span class="fn">box</span>(w, h, t);
<span class="kw">const</span> hole = <span class="fn">cylinder</span>(t + <span class="num">2</span>, <span class="num">4</span>).<span class="fn">translate</span>(w/<span class="num">2</span>, h/<span class="num">2</span>, -<span class="num">1</span>);
<span class="kw">return</span> base.<span class="fn">subtract</span>(hole).<span class="fn">fillet</span>(<span class="num">1</span>);</pre>
    </section>

    <footer class="footer">
      <div>kernelCAD · MIT</div>
      <div class="footer-links">
        <a href="https://github.com/w1ne/kernelCAD-web">github</a>
        <a href="https://www.npmjs.com/package/kernelcad">npm</a>
        <a href="https://x.com/kernelcad">x</a>
        <a href="https://www.linkedin.com/company/kernelcad">linkedin</a>
        <a href="https://app.kernelcad.com">app</a>
      </div>
    </footer>

  </div>

  <script>
    // load demo video + version from build-time generated demo.json
    fetch('/demo.json')
      .then(r => r.ok ? r.json() : null)
      .then(d => {
        if (!d) return;
        document.getElementById('demo-video').src = '/demo.mp4';
        document.getElementById('demo-version').textContent = d.version;
      });

    // copy install command on click
    document.getElementById('install-btn').addEventListener('click', () => {
      navigator.clipboard.writeText('npm install -g kernelcad');
      const copy = document.querySelector('.install .copy');
      const orig = copy.textContent;
      copy.textContent = 'copied';
      setTimeout(() => { copy.textContent = orig; }, 1200);
    });
  </script>
</body>
</html>
```

- [ ] **Step 2: Verify HTML opens (no styling yet)**

```bash
cd ~/projects/kernelCAD-web/site && python3 -m http.server 8000 &
SERVER_PID=$!
sleep 1
curl -sI http://localhost:8000/ | head -1
kill $SERVER_PID
```

Expected: `HTTP/1.0 200 OK`

- [ ] **Step 3: Commit**

```bash
git add site/index.html
git commit -m "feat(website): landing page HTML structure"
```

---

## Task 3: Landing page CSS

**Files:**
- Create: `site/style.css`

- [ ] **Step 1: Write `site/style.css`**

```css
:root {
  --vellum: #F4ECD7;
  --vellum-soft: #EFE5C9;
  --ink: #0A1628;
  --ink-soft: #3F4C5E;
  --ink-faint: #97A0AC;
  --blueprint: #1E5FA8;
  --blueprint-hover: #174E8B;
  --copper: #B87333;
  --rule: #D6CDB4;
  --code-bg: #0A1628;
  --code-text: #E5DFCB;
}

* { box-sizing: border-box; }

html, body { margin: 0; padding: 0; }

body {
  background: var(--vellum);
  color: var(--ink);
  font-family: 'Inter', -apple-system, system-ui, sans-serif;
  line-height: 1.5;
  font-size: 16px;
  -webkit-font-smoothing: antialiased;
}

.page-wrap { max-width: 1040px; margin: 0 auto; padding: 28px 40px; }

/* NAV */
.nav { display: flex; justify-content: space-between; align-items: center; padding-bottom: 100px; }
.nav-mark {
  display: flex; align-items: center; gap: 10px;
  font-family: 'Source Serif 4', Georgia, serif; font-size: 18px; font-weight: 500;
  color: var(--ink); text-decoration: none;
}
.nav-mark .k { width: 22px; height: 22px; color: var(--ink); }
.nav-mark .cad { color: var(--blueprint); }
.nav-links { display: flex; gap: 24px; font-family: 'JetBrains Mono', ui-monospace, monospace; font-size: 12px; color: var(--ink-soft); letter-spacing: 0.04em; }
.nav-links a { color: var(--ink-soft); text-decoration: none; }
.nav-links a:hover { color: var(--blueprint); }
.nav-links a.app-link { color: var(--blueprint); font-weight: 500; }

/* HERO */
.hero { text-align: center; padding: 0 0 72px; }
.headline {
  font-family: 'Source Serif 4', Georgia, serif;
  font-size: 96px; font-weight: 500; letter-spacing: -0.03em;
  line-height: 0.95; margin: 0 0 28px;
}
.headline .accent { color: var(--blueprint); font-style: italic; }
.subhead { font-size: 20px; color: var(--ink-soft); max-width: 560px; margin: 0 auto 16px; line-height: 1.5; }

.modes {
  font-family: 'JetBrains Mono', ui-monospace, monospace; font-size: 12px;
  color: var(--ink-faint); letter-spacing: 0.06em;
  margin: 0 auto 40px; display: inline-flex; gap: 14px;
}
.modes .mode { color: var(--ink-soft); }
.modes .sep { opacity: 0.5; }

.ctas-primary { display: flex; gap: 12px; justify-content: center; flex-wrap: wrap; margin-bottom: 14px; }
.btn {
  display: inline-flex; align-items: center; gap: 8px;
  padding: 13px 22px; border-radius: 8px;
  font-size: 15px; font-weight: 500; font-family: inherit;
  text-decoration: none; border: 1px solid; cursor: pointer;
  transition: background 0.15s, border-color 0.15s;
}
.btn-primary { background: var(--blueprint); color: #FFF; border-color: var(--blueprint); font-size: 16px; padding: 14px 24px; }
.btn-primary:hover { background: var(--blueprint-hover); border-color: var(--blueprint-hover); }
.btn-secondary { background: transparent; color: var(--ink); border-color: var(--rule); }
.btn-secondary:hover { border-color: var(--ink); }

.ctas-tertiary {
  display: flex; gap: 18px; justify-content: center; align-items: center;
  font-family: 'JetBrains Mono', ui-monospace, monospace; font-size: 12px;
  color: var(--ink-soft); letter-spacing: 0.04em;
  margin-bottom: 24px;
}
.ctas-tertiary a { color: var(--ink-soft); text-decoration: none; }
.ctas-tertiary a:hover { color: var(--blueprint); }
.ctas-tertiary .sep { opacity: 0.4; }

.install {
  display: inline-flex; align-items: center; gap: 16px;
  background: var(--code-bg); color: var(--code-text);
  padding: 10px 16px; border-radius: 8px;
  font-family: 'JetBrains Mono', ui-monospace, monospace; font-size: 13px;
  border: none; cursor: pointer;
}
.install:hover { background: #0F1E33; }
.install .prompt { color: var(--copper); }
.install .copy { color: var(--ink-faint); font-size: 11px; padding-left: 8px; border-left: 1px solid #1F2A3D; }

/* DEMO */
.demo-block { margin: 0 -20px 100px; }
.demo-frame {
  aspect-ratio: 16/10;
  background: linear-gradient(180deg, #06101F 0%, #0A1628 100%);
  border-radius: 14px; position: relative; overflow: hidden;
  display: flex; align-items: center; justify-content: center;
  box-shadow: 0 30px 80px -30px rgba(10,22,40,0.30);
  margin-top: 10px;
}
.demo-frame video { width: 100%; height: 100%; object-fit: cover; }
.demo-meta {
  display: flex; justify-content: space-between; align-items: center;
  font-family: 'JetBrains Mono', ui-monospace, monospace; font-size: 11px;
  color: var(--ink-soft); letter-spacing: 0.04em;
  padding: 0 20px;
}
.demo-meta .dot { display: inline-block; width: 6px; height: 6px; background: var(--blueprint); border-radius: 50%; margin-right: 8px; vertical-align: middle; }

/* CODE */
.code-block { margin: 0 0 100px; max-width: 720px; margin-left: auto; margin-right: auto; }
.code-label {
  font-family: 'JetBrains Mono', ui-monospace, monospace; font-size: 11px; color: var(--ink-soft);
  letter-spacing: 0.06em; margin-bottom: 14px; text-transform: uppercase;
  display: flex; align-items: center; gap: 10px;
}
.code-label::before { content: ''; width: 14px; height: 1px; background: var(--rule); }
.code {
  background: var(--code-bg); color: var(--code-text);
  padding: 26px 32px; border-radius: 10px;
  font-family: 'JetBrains Mono', ui-monospace, monospace; font-size: 13px; line-height: 1.7;
  white-space: pre-wrap; margin: 0; overflow-x: auto;
}
.code .kw  { color: #B5763B; }
.code .fn  { color: #6FA8D6; }
.code .num { color: #C2D6EE; }
.code .com { color: #5C6677; }
.code .str { color: #D6BC7A; }

/* FOOTER */
.footer {
  display: flex; justify-content: space-between; align-items: center;
  border-top: 1px solid var(--rule); padding: 28px 0; margin-top: 60px;
  font-family: 'JetBrains Mono', ui-monospace, monospace; font-size: 11px;
  color: var(--ink-soft); letter-spacing: 0.04em;
}
.footer-links { display: flex; gap: 22px; }
.footer-links a { color: var(--ink-soft); text-decoration: none; }
.footer-links a:hover { color: var(--blueprint); }

/* MOBILE */
@media (max-width: 768px) {
  .page-wrap { padding: 20px 20px; }
  .nav { padding-bottom: 60px; flex-wrap: wrap; gap: 12px; }
  .nav-links { font-size: 11px; gap: 14px; }
  .headline { font-size: 56px; }
  .subhead { font-size: 17px; }
  .demo-block { margin: 0 0 60px; }
  .code { font-size: 12px; padding: 20px; }
  .footer { flex-direction: column; gap: 12px; }
}
```

- [ ] **Step 2: Verify in browser**

```bash
cd ~/projects/kernelCAD-web/site && python3 -m http.server 8000 &
SERVER_PID=$!
sleep 1
echo "Open http://localhost:8000 — confirm: navy ink on vellum bg, blueprint blue 'Try in browser' button, italic blueprint 'agents' in headline, demo frame is dark placeholder box (no video yet — demo.json doesn't exist)."
read -p "Press enter when verified..."
kill $SERVER_PID
```

- [ ] **Step 3: Commit**

```bash
git add site/style.css
git commit -m "feat(website): landing page styles (Vellum palette)"
```

---

## Task 4: Favicon

**Files:**
- Create: `site/public/favicon.svg`

- [ ] **Step 1: Write `site/public/favicon.svg`**

```xml
<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 84 84">
  <rect width="84" height="84" fill="#F4ECD7"/>
  <path d="M 14,12 L 26,12 L 26,34 Q 26,36 27.5,34.5 L 46,12 L 60,12 L 36,40 Q 35,42 36,44 L 60,72 L 46,72 L 27.5,49.5 Q 26,48 26,50 L 26,72 L 14,72 Z" fill="#0A1628"/>
</svg>
```

- [ ] **Step 2: Verify favicon loads**

```bash
cd ~/projects/kernelCAD-web/site && python3 -m http.server 8000 &
SERVER_PID=$!
sleep 1
curl -sI http://localhost:8000/public/favicon.svg | head -1
echo "Open http://localhost:8000 — verify the K appears in the browser tab."
read -p "Press enter when verified..."
kill $SERVER_PID
```

Expected: `HTTP/1.0 200 OK`. Tab shows K mark.

- [ ] **Step 3: Move favicon to served path**

The HTML references `/favicon.svg` not `/public/favicon.svg`. Cloudflare Pages serves `site/public/` at the root, but the local `python -m http.server` does not. Fix by serving from `site/` and ensuring the favicon is at `site/public/favicon.svg` — the local URL becomes `http://localhost:8000/public/favicon.svg`. Update HTML for local dev OR rely on prod path matching.

Decision: keep `<link rel="icon" href="/favicon.svg" />` in HTML (production-correct). For local dev, document that the favicon shows on the deployed site, not the python http.server. No change needed.

- [ ] **Step 4: Commit**

```bash
git add site/public/favicon.svg
git commit -m "feat(website): K-mark favicon"
```

---

## Task 5: Demo auto-discovery script (TDD)

**Files:**
- Create: `site/scripts/build-demo.mjs`
- Create: `site/scripts/build-demo.test.mjs`

- [ ] **Step 1: Write the failing test**

Write `site/scripts/build-demo.test.mjs`:

```js
import { test } from 'node:test';
import assert from 'node:assert/strict';
import { findLatestDemo, parseSemver } from './build-demo.mjs';

test('parseSemver parses v0.21', () => {
  assert.deepEqual(parseSemver('v0.21'), [0, 21, 0]);
});

test('parseSemver parses v1.2.3', () => {
  assert.deepEqual(parseSemver('v1.2.3'), [1, 2, 3]);
});

test('parseSemver returns null for non-version dirs', () => {
  assert.equal(parseSemver('README.md'), null);
  assert.equal(parseSemver('vNotAVersion'), null);
});

test('findLatestDemo picks highest semver from a list', () => {
  const dirs = ['v0.1', 'v0.21', 'v0.2', 'README.md'];
  assert.equal(findLatestDemo(dirs), 'v0.21');
});

test('findLatestDemo handles single entry', () => {
  assert.equal(findLatestDemo(['v0.1']), 'v0.1');
});

test('findLatestDemo returns null on empty list', () => {
  assert.equal(findLatestDemo([]), null);
});
```

- [ ] **Step 2: Run test (must fail with module-not-found)**

```bash
cd ~/projects/kernelCAD-web && node --test site/scripts/build-demo.test.mjs
```

Expected: `Cannot find module './build-demo.mjs'`. That confirms the test runs.

- [ ] **Step 3: Implement `site/scripts/build-demo.mjs`**

Write `site/scripts/build-demo.mjs`:

```js
#!/usr/bin/env node
// Picks the highest-semver demo from ../docs/demos/v*/, copies the MP4 into
// site/public/demo.mp4, and writes site/public/demo.json with metadata.

import fs from 'node:fs';
import path from 'node:path';
import { fileURLToPath } from 'node:url';

const __dirname = path.dirname(fileURLToPath(import.meta.url));
const DEMOS_ROOT = path.resolve(__dirname, '../../docs/demos');
const PUBLIC_DIR = path.resolve(__dirname, '../public');

export function parseSemver(name) {
  const m = /^v(\d+)\.(\d+)(?:\.(\d+))?$/.exec(name);
  if (!m) return null;
  return [Number(m[1]), Number(m[2]), Number(m[3] ?? 0)];
}

function compareSemver(a, b) {
  for (let i = 0; i < 3; i++) {
    if (a[i] !== b[i]) return a[i] - b[i];
  }
  return 0;
}

export function findLatestDemo(entries) {
  const versioned = entries
    .map(name => ({ name, ver: parseSemver(name) }))
    .filter(e => e.ver !== null);
  if (versioned.length === 0) return null;
  versioned.sort((a, b) => compareSemver(b.ver, a.ver));
  return versioned[0].name;
}

function findFirstMp4(dir) {
  // walks one level deep; demo dirs are <root>/<v>/<task>/*.mp4
  for (const entry of fs.readdirSync(dir, { withFileTypes: true })) {
    if (entry.isDirectory()) {
      const sub = path.join(dir, entry.name);
      for (const f of fs.readdirSync(sub)) {
        if (f.endsWith('.mp4')) return path.join(sub, f);
      }
    } else if (entry.name.endsWith('.mp4')) {
      return path.join(dir, entry.name);
    }
  }
  return null;
}

async function main() {
  if (!fs.existsSync(DEMOS_ROOT)) {
    console.error(`demos root not found: ${DEMOS_ROOT}`);
    process.exit(1);
  }
  const entries = fs.readdirSync(DEMOS_ROOT);
  const latest = findLatestDemo(entries);
  if (!latest) {
    console.error(`no v*/ directories under ${DEMOS_ROOT}`);
    process.exit(1);
  }
  const demoDir = path.join(DEMOS_ROOT, latest);
  const mp4 = findFirstMp4(demoDir);
  if (!mp4) {
    console.error(`no .mp4 found under ${demoDir}`);
    process.exit(1);
  }

  fs.mkdirSync(PUBLIC_DIR, { recursive: true });
  const dest = path.join(PUBLIC_DIR, 'demo.mp4');
  fs.copyFileSync(mp4, dest);

  const meta = {
    version: latest,
    source: path.relative(path.resolve(__dirname, '../..'), mp4),
    captured_at: new Date().toISOString(),
  };
  fs.writeFileSync(path.join(PUBLIC_DIR, 'demo.json'), JSON.stringify(meta, null, 2));

  console.log(`✓ ${latest}: copied ${path.basename(mp4)} → site/public/demo.mp4`);
}

if (import.meta.url === `file://${process.argv[1]}`) {
  main();
}
```

- [ ] **Step 4: Run tests (must pass)**

```bash
cd ~/projects/kernelCAD-web && node --test site/scripts/build-demo.test.mjs
```

Expected: `# pass 6`, no failures.

- [ ] **Step 5: Run the script for real**

```bash
cd ~/projects/kernelCAD-web && node site/scripts/build-demo.mjs
ls -la site/public/demo.mp4 site/public/demo.json
cat site/public/demo.json
```

Expected: `✓ v0.21: copied <name>.mp4 → site/public/demo.mp4`. demo.json contains `{"version": "v0.21", "source": "...", "captured_at": "..."}`. demo.mp4 exists with non-zero size.

- [ ] **Step 6: Add demo.mp4 to .gitignore (it's a build artifact, often >1MB)**

Append to `.gitignore`:

```
site/public/demo.mp4
site/public/demo.json
```

- [ ] **Step 7: Commit**

```bash
git add site/scripts/build-demo.mjs site/scripts/build-demo.test.mjs .gitignore
git commit -m "feat(website): demo auto-discovery (highest semver from docs/demos/v*/)"
```

---

## Task 6: Verify demo plays in landing page

**Files:**
- (verification only — no new files)

- [ ] **Step 1: Run build script + serve site**

```bash
cd ~/projects/kernelCAD-web && node site/scripts/build-demo.mjs
cd site && python3 -m http.server 8000 &
SERVER_PID=$!
sleep 1
echo "Open http://localhost:8000 — verify demo video loads, autoplays muted, loops, and version badge reads 'v0.21 · live build'."
read -p "Press enter when verified..."
kill $SERVER_PID
```

Note: `python3 -m http.server` serves the cwd, so from `site/` the URLs `/demo.mp4` and `/demo.json` map to `site/demo.mp4` and `site/demo.json`. The build script writes them to `site/public/`. Two options:

**Option A (chosen):** Serve from `site/public/` for local dev. Update the dev command in README.

**Option B:** Move `index.html` and `style.css` into `public/`. Less surprising for static-site conventions but requires restructuring.

Choose Option A — add a one-line note to `site/README.md` that the public/ folder serves to root in production.

- [ ] **Step 2: Update site/README.md dev instructions**

Edit `site/README.md` — replace the Develop block with:

```markdown
## Develop

```bash
node site/scripts/build-demo.mjs       # generate demo.mp4 + demo.json into public/
cd site && python3 -m http.server 8000 -d public
# this won't serve index.html — instead use:
# (cd site && python3 -m http.server 8000)  to view markup, knowing /demo.* won't load locally
# Production: Cloudflare Pages serves public/ at root and index.html alongside.
```

For full local fidelity, copy public/ contents next to index.html before serving:
```bash
node site/scripts/build-demo.mjs && cp site/public/* site/ && cd site && python3 -m http.server 8000
```
```

Actually simpler — symlink public/ contents up:

```bash
cd site && for f in public/*; do ln -sf "$f" "$(basename "$f")"; done
```

Add `site/demo.mp4`, `site/demo.json`, `site/favicon.svg` (the symlinks) to `.gitignore`:

```
site/demo.mp4
site/demo.json
site/favicon.svg
```

(Cloudflare Pages handles this in production by serving `public/` at root automatically — the symlinks are dev-only.)

- [ ] **Step 3: Verify with symlinks in place**

```bash
cd ~/projects/kernelCAD-web && node site/scripts/build-demo.mjs
cd site && ln -sf public/demo.mp4 demo.mp4 && ln -sf public/demo.json demo.json && ln -sf public/favicon.svg favicon.svg
python3 -m http.server 8000 &
SERVER_PID=$!
sleep 1
curl -sI http://localhost:8000/demo.json | head -1
echo "Open http://localhost:8000 — confirm video plays + version reads v0.21."
read -p "Press enter when verified..."
kill $SERVER_PID
```

Expected: 200 on demo.json. Video plays in browser.

- [ ] **Step 4: Commit**

```bash
git add site/README.md .gitignore
git commit -m "docs(website): document local-dev symlink workflow for public/ assets"
```

---

## Task 7: Brand templates — shared CSS

**Files:**
- Create: `site/brand-templates/_shared.css`

- [ ] **Step 1: Write `site/brand-templates/_shared.css`**

```css
/* Shared palette + grid for all brand templates. Each template loads this. */
:root {
  --vellum: #F4ECD7;
  --ink: #0A1628;
  --ink-soft: #3F4C5E;
  --ink-faint: #97A0AC;
  --blueprint: #1E5FA8;
  --copper: #B87333;
  --rule: #D6CDB4;
  --grid-line: rgba(10, 22, 40, 0.05);
}

* { box-sizing: border-box; }
html, body { margin: 0; padding: 0; }

@font-face {
  font-family: 'Source Serif 4';
  src: local('Source Serif 4'), local('Source Serif Pro');
  font-weight: 500;
}

.brand-canvas {
  background: var(--vellum);
  background-image:
    linear-gradient(to right, var(--grid-line) 1px, transparent 1px),
    linear-gradient(to bottom, var(--grid-line) 1px, transparent 1px);
  position: relative;
  overflow: hidden;
  font-family: 'Inter', system-ui, sans-serif;
  color: var(--ink);
}

.serif {
  font-family: 'Source Serif 4', 'Source Serif Pro', Georgia, serif;
  font-weight: 500;
  letter-spacing: -0.02em;
}
.italic { font-style: italic; }
.mono { font-family: 'JetBrains Mono', ui-monospace, monospace; letter-spacing: 0.04em; }
.cad { color: var(--blueprint); }
```

- [ ] **Step 2: Commit**

```bash
git add site/brand-templates/_shared.css
git commit -m "feat(website): shared brand template styles"
```

---

## Task 8: Brand template — avatar (light + dark)

**Files:**
- Create: `site/brand-templates/avatar.html`
- Create: `site/brand-templates/avatar-dark.html`

- [ ] **Step 1: Write `site/brand-templates/avatar.html`**

```html
<!DOCTYPE html>
<html>
<head>
<meta charset="utf-8">
<link rel="stylesheet" href="_shared.css">
<style>
  body { width: 400px; height: 400px; }
  .canvas {
    width: 400px; height: 400px;
    background-size: 22px 22px;
    display: flex; align-items: center; justify-content: center;
  }
  .canvas svg { width: 56%; height: 56%; }
</style>
</head>
<body>
  <div class="brand-canvas canvas">
    <svg viewBox="0 0 84 84" fill="none" xmlns="http://www.w3.org/2000/svg">
      <path d="M 14,12 L 26,12 L 26,34 Q 26,36 27.5,34.5 L 46,12 L 60,12 L 36,40 Q 35,42 36,44 L 60,72 L 46,72 L 27.5,49.5 Q 26,48 26,50 L 26,72 L 14,72 Z" fill="#0A1628"/>
    </svg>
  </div>
</body>
</html>
```

- [ ] **Step 2: Write `site/brand-templates/avatar-dark.html`**

```html
<!DOCTYPE html>
<html>
<head>
<meta charset="utf-8">
<link rel="stylesheet" href="_shared.css">
<style>
  body { width: 400px; height: 400px; }
  .canvas {
    width: 400px; height: 400px;
    background: #0A1628 !important;
    background-image:
      linear-gradient(to right, rgba(244,236,215,0.04) 1px, transparent 1px),
      linear-gradient(to bottom, rgba(244,236,215,0.04) 1px, transparent 1px) !important;
    background-size: 22px 22px !important;
    display: flex; align-items: center; justify-content: center;
  }
  .canvas svg { width: 56%; height: 56%; }
</style>
</head>
<body>
  <div class="brand-canvas canvas">
    <svg viewBox="0 0 84 84" fill="none" xmlns="http://www.w3.org/2000/svg">
      <path d="M 14,12 L 26,12 L 26,34 Q 26,36 27.5,34.5 L 46,12 L 60,12 L 36,40 Q 35,42 36,44 L 60,72 L 46,72 L 27.5,49.5 Q 26,48 26,50 L 26,72 L 14,72 Z" fill="#F4ECD7"/>
    </svg>
  </div>
</body>
</html>
```

- [ ] **Step 3: Verify in browser**

```bash
cd ~/projects/kernelCAD-web/site && python3 -m http.server 8000 &
SERVER_PID=$!
sleep 1
echo "Open http://localhost:8000/brand-templates/avatar.html — should be a 400×400 vellum square with K mark centered."
echo "Open http://localhost:8000/brand-templates/avatar-dark.html — should be 400×400 navy square with vellum K."
read -p "Press enter when verified..."
kill $SERVER_PID
```

- [ ] **Step 4: Commit**

```bash
git add site/brand-templates/avatar.html site/brand-templates/avatar-dark.html
git commit -m "feat(website): avatar brand templates (light + dark)"
```

---

## Task 9: Brand template — X header (1500×500)

**Files:**
- Create: `site/brand-templates/x-header.html`

- [ ] **Step 1: Write `site/brand-templates/x-header.html`**

```html
<!DOCTYPE html>
<html>
<head>
<meta charset="utf-8">
<link rel="stylesheet" href="_shared.css">
<style>
  body { width: 1500px; height: 500px; }
  .canvas {
    width: 1500px; height: 500px;
    background-size: 28px 28px;
    position: relative;
  }
  .content {
    position: absolute; inset: 0;
    display: flex; align-items: center;
    padding: 0 105px;
  }
  .content-inner {
    margin-left: 330px;  /* 22% — clears bottom-left avatar zone */
    display: flex; align-items: center; gap: 75px;
  }
  .mark { width: 270px; height: 270px; }
  .mark svg { width: 100%; height: 100%; }
  .text { display: flex; flex-direction: column; gap: 8px; }
  .wordmark { font-size: 96px; line-height: 1; color: var(--ink); }
  .tagline { font-size: 50px; line-height: 1; color: var(--blueprint); margin-top: 6px; }
  .modes { font-size: 22px; color: var(--ink-soft); margin-top: 16px; }
  .modes span.sep { opacity: 0.5; margin: 0 6px; }
  .url {
    position: absolute; top: 30px; right: 50px;
    font-size: 20px; color: var(--ink-soft);
  }
  .dim {
    position: absolute; bottom: 40px; right: 50px;
    display: flex; align-items: center; gap: 8px;
    font-size: 18px; color: var(--ink-soft);
  }
  .dim .line { width: 80px; height: 1px; background: currentColor; }
  .dim .tick { width: 1px; height: 8px; background: currentColor; }
</style>
</head>
<body>
  <div class="brand-canvas canvas">
    <div class="url mono">kernelcad.com</div>
    <div class="content">
      <div class="content-inner">
        <div class="mark">
          <svg viewBox="0 0 84 84" fill="none" xmlns="http://www.w3.org/2000/svg">
            <path d="M 14,12 L 26,12 L 26,34 Q 26,36 27.5,34.5 L 46,12 L 60,12 L 36,40 Q 35,42 36,44 L 60,72 L 46,72 L 27.5,49.5 Q 26,48 26,50 L 26,72 L 14,72 Z" fill="#0A1628"/>
            <g stroke="#1E5FA8" stroke-width="0.6" fill="#1E5FA8">
              <line x1="34" y1="42" x2="78" y2="42"/>
              <text x="80" y="40" font-family="JetBrains Mono, monospace" font-size="6" stroke="none" text-anchor="end">R 2</text>
            </g>
          </svg>
        </div>
        <div class="text">
          <div class="serif wordmark">kernel<span class="cad">CAD</span></div>
          <div class="serif italic tagline">CAD for agents.</div>
          <div class="mono modes">
            <span>browser</span><span class="sep">·</span>
            <span>terminal</span><span class="sep">·</span>
            <span>MCP</span><span class="sep">·</span>
            <span>open source</span>
          </div>
        </div>
      </div>
    </div>
    <div class="dim mono">
      <span class="tick"></span><span class="line"></span>
      <span>v0.21</span>
      <span class="line"></span><span class="tick"></span>
    </div>
  </div>
</body>
</html>
```

- [ ] **Step 2: Verify in browser**

```bash
cd ~/projects/kernelCAD-web/site && python3 -m http.server 8000 &
SERVER_PID=$!
sleep 1
echo "Open http://localhost:8000/brand-templates/x-header.html — confirm 1500×500 layout: K mark with R 2 callout left-of-center, wordmark + 'CAD for agents.' italic tagline + modes line right of mark, kernelcad.com top-right, v0.21 dim bottom-right. Bottom-left empty (avatar overlay zone)."
read -p "Press enter when verified..."
kill $SERVER_PID
```

- [ ] **Step 3: Commit**

```bash
git add site/brand-templates/x-header.html
git commit -m "feat(website): X header template (1500x500)"
```

---

## Task 10: Brand template — LinkedIn cover (1128×191)

**Files:**
- Create: `site/brand-templates/linkedin-cover.html`

- [ ] **Step 1: Write `site/brand-templates/linkedin-cover.html`**

```html
<!DOCTYPE html>
<html>
<head>
<meta charset="utf-8">
<link rel="stylesheet" href="_shared.css">
<style>
  body { width: 1128px; height: 191px; }
  .canvas {
    width: 1128px; height: 191px;
    background-size: 22px 22px;
    position: relative;
  }
  .content {
    position: absolute; inset: 0;
    display: flex; align-items: center; justify-content: space-between;
    padding: 0 38px 0 180px;  /* 180px left = clear of logo overlay zone */
  }
  .text { display: flex; align-items: baseline; gap: 22px; }
  .wordmark { font-size: 50px; line-height: 1; color: var(--ink); }
  .tagline { font-size: 30px; line-height: 1; color: var(--blueprint); }
  .right { text-align: right; line-height: 1.4; }
  .url-line { font-size: 18px; color: var(--ink); font-weight: 500; }
  .modes-line { font-size: 14px; color: var(--ink-soft); margin-top: 4px; }
</style>
</head>
<body>
  <div class="brand-canvas canvas">
    <div class="content">
      <div class="text">
        <span class="serif wordmark">kernel<span class="cad">CAD</span></span>
        <span class="serif italic tagline">CAD for agents.</span>
      </div>
      <div class="right">
        <div class="mono url-line">kernelcad.com</div>
        <div class="mono modes-line">browser · terminal · MCP</div>
      </div>
    </div>
  </div>
</body>
</html>
```

- [ ] **Step 2: Verify in browser**

```bash
cd ~/projects/kernelCAD-web/site && python3 -m http.server 8000 &
SERVER_PID=$!
sleep 1
echo "Open http://localhost:8000/brand-templates/linkedin-cover.html — confirm 1128×191 horizontal strip: wordmark + tagline left (after 180px logo zone), URL + modes right."
read -p "Press enter when verified..."
kill $SERVER_PID
```

- [ ] **Step 3: Commit**

```bash
git add site/brand-templates/linkedin-cover.html
git commit -m "feat(website): LinkedIn company cover template (1128x191)"
```

---

## Task 11: Brand template — OG image (1280×640)

**Files:**
- Create: `site/brand-templates/og-image.html`

- [ ] **Step 1: Write `site/brand-templates/og-image.html`**

Same composition as X header but reflowed for 1280×640 (2:1 instead of 3:1). Centered layout (no avatar zone to dodge).

```html
<!DOCTYPE html>
<html>
<head>
<meta charset="utf-8">
<link rel="stylesheet" href="_shared.css">
<style>
  body { width: 1280px; height: 640px; }
  .canvas {
    width: 1280px; height: 640px;
    background-size: 28px 28px;
    position: relative;
    display: flex; align-items: center; justify-content: center;
  }
  .content { display: flex; flex-direction: column; align-items: center; gap: 18px; }
  .mark { width: 180px; height: 180px; }
  .wordmark { font-size: 110px; line-height: 1; color: var(--ink); }
  .tagline { font-size: 60px; line-height: 1; color: var(--blueprint); }
  .modes { font-size: 22px; color: var(--ink-soft); margin-top: 12px; }
  .modes .sep { opacity: 0.5; margin: 0 8px; }
  .url {
    position: absolute; bottom: 40px; right: 50px;
    font-size: 22px; color: var(--ink-soft);
  }
</style>
</head>
<body>
  <div class="brand-canvas canvas">
    <div class="content">
      <div class="mark">
        <svg viewBox="0 0 84 84" fill="none" xmlns="http://www.w3.org/2000/svg">
          <path d="M 14,12 L 26,12 L 26,34 Q 26,36 27.5,34.5 L 46,12 L 60,12 L 36,40 Q 35,42 36,44 L 60,72 L 46,72 L 27.5,49.5 Q 26,48 26,50 L 26,72 L 14,72 Z" fill="#0A1628"/>
        </svg>
      </div>
      <div class="serif wordmark">kernel<span class="cad">CAD</span></div>
      <div class="serif italic tagline">CAD for agents.</div>
      <div class="mono modes">
        <span>browser</span><span class="sep">·</span>
        <span>terminal</span><span class="sep">·</span>
        <span>MCP</span><span class="sep">·</span>
        <span>open source</span>
      </div>
    </div>
    <div class="url mono">kernelcad.com</div>
  </div>
</body>
</html>
```

- [ ] **Step 2: Verify in browser**

```bash
cd ~/projects/kernelCAD-web/site && python3 -m http.server 8000 &
SERVER_PID=$!
sleep 1
echo "Open http://localhost:8000/brand-templates/og-image.html — confirm centered layout, K mark + wordmark stacked, kernelcad.com bottom-right."
read -p "Press enter when verified..."
kill $SERVER_PID
```

- [ ] **Step 3: Commit**

```bash
git add site/brand-templates/og-image.html
git commit -m "feat(website): OG/social-preview brand template (1280x640)"
```

---

## Task 12: Brand renderer (Playwright)

**Files:**
- Create: `site/scripts/render-brand.mjs`

- [ ] **Step 1: Verify Playwright is installed**

```bash
cd ~/projects/kernelCAD-web && npm ls @playwright/test
```

Expected: `@playwright/test@1.58.x` listed.

- [ ] **Step 2: Verify a Chromium binary is available to Playwright**

```bash
cd ~/projects/kernelCAD-web && npx playwright install chromium 2>&1 | tail -3
```

Expected: either "is already installed" or successful download. (If a corporate proxy blocks the download, fall back to `playwright-chromium` package or document the manual install path.)

- [ ] **Step 3: Write `site/scripts/render-brand.mjs`**

```js
#!/usr/bin/env node
// Renders brand HTML templates to PNG via Playwright (headless Chromium).
// Outputs land in site/public/brand/ AND site/public/og-image.png at root.

import path from 'node:path';
import fs from 'node:fs';
import { fileURLToPath, pathToFileURL } from 'node:url';
import { chromium } from 'playwright';

const __dirname = path.dirname(fileURLToPath(import.meta.url));
const TEMPLATES_DIR = path.resolve(__dirname, '../brand-templates');
const PUBLIC_DIR = path.resolve(__dirname, '../public');
const BRAND_OUT = path.join(PUBLIC_DIR, 'brand');

const TARGETS = [
  { template: 'avatar.html',         out: 'avatar-light.png',     w: 400,  h: 400 },
  { template: 'avatar-dark.html',    out: 'avatar-dark.png',      w: 400,  h: 400 },
  { template: 'x-header.html',       out: 'x-header.png',         w: 1500, h: 500 },
  { template: 'linkedin-cover.html', out: 'linkedin-cover.png',   w: 1128, h: 191 },
  { template: 'og-image.html',       out: 'og-image.png',         w: 1280, h: 640 },
];

async function main() {
  fs.mkdirSync(BRAND_OUT, { recursive: true });
  const browser = await chromium.launch();
  for (const t of TARGETS) {
    const templatePath = path.join(TEMPLATES_DIR, t.template);
    if (!fs.existsSync(templatePath)) {
      console.error(`✗ missing template: ${templatePath}`);
      continue;
    }
    const ctx = await browser.newContext({
      viewport: { width: t.w, height: t.h },
      deviceScaleFactor: 2,  // 2x for retina-quality PNGs
    });
    const page = await ctx.newPage();
    await page.goto(pathToFileURL(templatePath).href);
    // Wait for fonts to fully load before screenshot
    await page.evaluate(() => document.fonts.ready);
    const outPath = path.join(BRAND_OUT, t.out);
    await page.screenshot({ path: outPath, omitBackground: false });
    await ctx.close();
    const size = fs.statSync(outPath).size;
    console.log(`✓ ${t.template} → brand/${t.out} (${t.w}×${t.h}@2x, ${(size/1024).toFixed(1)} KB)`);
  }
  await browser.close();

  // Copy og-image.png to public/ root for OG meta tag
  fs.copyFileSync(path.join(BRAND_OUT, 'og-image.png'), path.join(PUBLIC_DIR, 'og-image.png'));
  console.log(`✓ copied og-image.png → public/og-image.png`);
}

main().catch(e => { console.error(e); process.exit(1); });
```

- [ ] **Step 4: Run the renderer**

```bash
cd ~/projects/kernelCAD-web && node site/scripts/render-brand.mjs
ls -la site/public/brand/
```

Expected: 5 PNG files in `site/public/brand/`:
- `avatar-light.png` (~400×400 displayed = 800×800 actual due to 2x DPR)
- `avatar-dark.png`
- `x-header.png` (1500×500 displayed = 3000×1000 actual)
- `linkedin-cover.png`
- `og-image.png`

Plus `site/public/og-image.png` copy.

- [ ] **Step 5: Visually inspect rendered PNGs**

```bash
xdg-open site/public/brand/x-header.png  # or open one at a time
xdg-open site/public/brand/avatar-light.png
xdg-open site/public/brand/linkedin-cover.png
echo "Verify: PNGs match the browser preview — vellum bg, navy K, blueprint accents, fonts rendered correctly."
read -p "Press enter when satisfied..."
```

If fonts look wrong (system fallbacks instead of Source Serif), the page didn't wait long enough — increase the `document.fonts.ready` wait or add an explicit `await page.waitForTimeout(500)`.

- [ ] **Step 6: Add brand/ to gitignore (PNG outputs are build artifacts)**

Append to `.gitignore`:

```
site/public/brand/
site/public/og-image.png
```

- [ ] **Step 7: Commit**

```bash
git add site/scripts/render-brand.mjs .gitignore
git commit -m "feat(website): Playwright brand renderer (avatar/X/LinkedIn/OG PNGs)"
```

---

## Task 13: Wire build scripts into package.json

**Files:**
- Modify: `package.json`

- [ ] **Step 1: Read current scripts section**

```bash
cd ~/projects/kernelCAD-web && grep -A 30 '"scripts"' package.json | head -40
```

- [ ] **Step 2: Add four new scripts**

Use Edit tool on `package.json` to add to the `"scripts"` object (after the existing `"test:e2e"` line):

```json
    "site:dev": "node site/scripts/build-demo.mjs && cd site && (for f in public/*; do ln -sf \"$f\" \"$(basename \"$f\")\"; done) && python3 -m http.server 8000",
    "site:build": "node site/scripts/build-demo.mjs && node site/scripts/render-brand.mjs",
    "site:render-brand": "node site/scripts/render-brand.mjs",
    "site:test": "node --test site/scripts/build-demo.test.mjs",
```

- [ ] **Step 3: Verify all four scripts run cleanly**

```bash
cd ~/projects/kernelCAD-web
npm run site:test       # build-demo unit tests
npm run site:render-brand  # regenerate PNGs
```

Expected: tests pass, PNGs regenerate.

- [ ] **Step 4: Commit**

```bash
git add package.json
git commit -m "feat(website): npm scripts (site:dev / site:build / site:render-brand / site:test)"
```

---

## Task 14: Local end-to-end smoke test

**Files:**
- (verification only)

- [ ] **Step 1: Full local build + serve**

```bash
cd ~/projects/kernelCAD-web
npm run site:build
cd site && for f in public/*; do ln -sf "$f" "$(basename "$f")"; done
python3 -m http.server 8000 &
SERVER_PID=$!
sleep 1
echo "Open http://localhost:8000 and verify everything works:"
echo " - [ ] Vellum bg + navy ink + blueprint blue accent"
echo " - [ ] K mark loads in nav and tab favicon"
echo " - [ ] Headline reads 'CAD for agents.' with 'agents' italic blueprint"
echo " - [ ] Modes line: browser · terminal · MCP · open source"
echo " - [ ] 'Try in browser →' is the loud blueprint button"
echo " - [ ] Demo video autoplays, muted, loops"
echo " - [ ] Version badge reads 'v0.21 · live build'"
echo " - [ ] Code block has navy bg with syntax highlighting"
echo " - [ ] Click install pill → 'copy' changes to 'copied' for 1.2s"
echo " - [ ] All footer links point to expected URLs (right-click → open in new tab to confirm)"
read -p "Press enter when all 9 items verified..."
kill $SERVER_PID
```

- [ ] **Step 2: Inspect responsive layout**

Resize browser to ~375px wide. Verify:
- Headline drops to 56px and stays readable
- Nav wraps, links remain visible
- Buttons stack reasonably
- Demo video remains visible

- [ ] **Step 3: Commit any fixes**

If anything broke, fix and commit with `fix(website): <what>`.

---

## Task 15: Cloudflare Pages — kernelcad.com (marketing site)

**Files:**
- Create: `site/_headers` (Cloudflare Pages config for caching)

- [ ] **Step 1: Add cache headers**

Write `site/_headers`:

```
/*.png
  Cache-Control: public, max-age=86400
/*.svg
  Cache-Control: public, max-age=86400
/demo.mp4
  Cache-Control: public, max-age=300
/demo.json
  Cache-Control: public, max-age=60
/style.css
  Cache-Control: public, max-age=3600
```

(`demo.json` short-cached so version updates propagate within a minute.)

- [ ] **Step 2: Push current branch to remote**

```bash
cd ~/projects/kernelCAD-web && git push -u origin develop
```

(Skip if already pushed.)

- [ ] **Step 3: Create Cloudflare Pages project (USER ACTION)**

The user does this in the Cloudflare dashboard. Provide them this exact config:

```
Project name:      kernelcad-marketing
Production branch: develop  (or main once we cut over)
Build command:     node site/scripts/build-demo.mjs && node site/scripts/render-brand.mjs
Build output:      site
Root directory:    /
Env vars:          (none)
```

After build succeeds, the project gets a `kernelcad-marketing.pages.dev` URL. Verify it loads.

- [ ] **Step 4: Add custom domain in Cloudflare Pages**

In the Pages project → Custom domains → Add `kernelcad.com`. Cloudflare will generate the CNAME target (e.g., `kernelcad-marketing.pages.dev`).

- [ ] **Step 5: Add CNAME at Namecheap (USER ACTION)**

User does in Namecheap dashboard for `kernelcad.com`:

```
Type:  CNAME
Host:  @         (or 'www' if @ needs ALIAS)
Value: kernelcad-marketing.pages.dev
TTL:   Automatic
```

(If Namecheap rejects `@` for CNAME, switch to A records pointing at Cloudflare's IPs as documented in the Pages dashboard, or add `www` CNAME and a URL redirect from apex to www.)

- [ ] **Step 6: Verify DNS propagates**

```bash
dig kernelcad.com +short
curl -sI https://kernelcad.com | head -3
```

Expected: dig resolves to Cloudflare IP. curl returns 200.

- [ ] **Step 7: Commit `_headers`**

```bash
git add site/_headers
git commit -m "feat(website): Cloudflare Pages cache headers"
git push
```

---

## Task 16: Cloudflare Pages — app.kernelcad.com (visual debugger)

**Files:**
- (no new files — uses existing root Vite app)

- [ ] **Step 1: Create second Cloudflare Pages project (USER ACTION)**

```
Project name:      kernelcad-app
Production branch: develop
Build command:     npm install && npm run build
Build output:      dist
Root directory:    /
Env vars:          (none, unless visual debugger needs any)
```

After build succeeds, the project gets a `kernelcad-app.pages.dev` URL. Verify the visual debugger loads.

- [ ] **Step 2: Add `app.kernelcad.com` custom domain in CF Pages**

In `kernelcad-app` project → Custom domains → Add `app.kernelcad.com`.

- [ ] **Step 3: Add CNAME at Namecheap (USER ACTION)**

```
Type:  CNAME
Host:  app
Value: kernelcad-app.pages.dev
TTL:   Automatic
```

- [ ] **Step 4: Verify**

```bash
dig app.kernelcad.com +short
curl -sI https://app.kernelcad.com | head -3
```

Expected: 200. Visual debugger loads in browser.

- [ ] **Step 5: Confirm cross-link works from marketing site**

Open `https://kernelcad.com`, click "Try in browser →" — should land on `https://app.kernelcad.com` with the visual debugger.

---

## Task 17: Upload assets to X (chrome-devtools MCP)

**Files:**
- (no new files — automation only)

**Prerequisites:** User logged into x.com as @kernelcad in the browser that chrome-devtools MCP controls.

- [ ] **Step 1: Confirm chrome-devtools MCP is available**

Confirm `mcp__chrome-devtools__navigate_page`, `mcp__chrome-devtools__upload_file`, `mcp__chrome-devtools__click`, `mcp__chrome-devtools__take_snapshot` tools are accessible. If not, ask user to enable the chrome-devtools MCP server.

- [ ] **Step 2: Open X profile settings**

```
Use mcp__chrome-devtools__navigate_page to https://x.com/settings/profile
Use mcp__chrome-devtools__take_snapshot to confirm logged-in state
```

If not logged in: stop. Ask user to log in and resume.

- [ ] **Step 3: Upload profile picture (avatar-light.png)**

The X profile-photo upload control is typically a hidden file input triggered by clicking the avatar. Steps:

```
mcp__chrome-devtools__click on the avatar / profile picture area
mcp__chrome-devtools__upload_file with absolute path:
  /home/andrii/projects/kernelCAD-web/site/public/brand/avatar-light.png
```

X presents a crop dialog — accept default crop or center-crop. Click "Apply" / "Save".

- [ ] **Step 4: Upload header (x-header.png)**

```
mcp__chrome-devtools__click on the header banner area
mcp__chrome-devtools__upload_file with absolute path:
  /home/andrii/projects/kernelCAD-web/site/public/brand/x-header.png
```

Accept the crop dialog. Click "Save".

- [ ] **Step 5: Verify**

```
mcp__chrome-devtools__navigate_page to https://x.com/kernelcad
mcp__chrome-devtools__take_screenshot to confirm new avatar + header are live
```

Inspect the screenshot — confirm the K mark avatar and the engineering-blueprint header are showing.

---

## Task 18: Upload assets to LinkedIn company page (chrome-devtools MCP)

**Files:**
- (no new files — automation only)

**Prerequisites:** User logged into LinkedIn with admin access to the kernelCAD company page.

- [ ] **Step 1: Open LinkedIn company admin**

```
mcp__chrome-devtools__navigate_page to https://www.linkedin.com/company/kernelcad/admin/
mcp__chrome-devtools__take_snapshot to confirm admin access
```

If page asks to log in or denies access: stop, ask user.

- [ ] **Step 2: Upload company logo (avatar-light.png)**

LinkedIn's "Edit page" → "Logo" section. Steps:

```
mcp__chrome-devtools__click on "Edit page" or the existing logo
mcp__chrome-devtools__upload_file with absolute path:
  /home/andrii/projects/kernelCAD-web/site/public/brand/avatar-light.png
mcp__chrome-devtools__click on "Save"
```

- [ ] **Step 3: Upload cover image (linkedin-cover.png)**

```
mcp__chrome-devtools__click on "Edit page" → cover image upload area
mcp__chrome-devtools__upload_file with absolute path:
  /home/andrii/projects/kernelCAD-web/site/public/brand/linkedin-cover.png
mcp__chrome-devtools__click on "Save"
```

- [ ] **Step 4: Verify**

```
mcp__chrome-devtools__navigate_page to https://www.linkedin.com/company/kernelcad/
mcp__chrome-devtools__take_screenshot to confirm new logo + cover are live
```

---

## Task 19: Upload GitHub social preview (chrome-devtools MCP)

**Files:**
- (no new files — automation only)

**Prerequisites:** User logged into GitHub with write access to `w1ne/kernelCAD-web`.

- [ ] **Step 1: Open repo settings**

```
mcp__chrome-devtools__navigate_page to https://github.com/w1ne/kernelCAD-web/settings
mcp__chrome-devtools__take_snapshot to confirm settings page loaded
```

- [ ] **Step 2: Scroll to "Social preview" section**

```
mcp__chrome-devtools__evaluate_script: document.querySelector('input[name="repository[image]"]')?.scrollIntoView()
```

- [ ] **Step 3: Upload OG image**

```
mcp__chrome-devtools__upload_file with absolute path:
  /home/andrii/projects/kernelCAD-web/site/public/brand/og-image.png
```

GitHub auto-saves on upload.

- [ ] **Step 4: Verify with social preview validator**

Open in user's browser:

```
https://opengraph.xyz/url/https%3A%2F%2Fgithub.com%2Fw1ne%2FkernelCAD-web
```

Expected: preview shows the OG image we uploaded.

---

## Task 20: Production smoke test

**Files:**
- (verification only)

- [ ] **Step 1: Anonymous visit to kernelcad.com**

In an incognito window:
- Open `https://kernelcad.com` — verify landing renders
- Click "Try in browser →" — verify lands on `https://app.kernelcad.com`
- Click "★ Star on GitHub" — verify lands on the repo
- Click "Follow on X" → @kernelcad profile loads with new avatar + header
- Click "Follow on LinkedIn" → company page loads with new logo + cover

- [ ] **Step 2: Demo auto-update verification**

Confirm the page shows the latest version. From the repo:

```bash
ls -d ~/projects/kernelCAD-web/docs/demos/v*/ | sort -V | tail -1
curl -s https://kernelcad.com/demo.json | grep version
```

Both should report the same version.

- [ ] **Step 3: Social card validators**

Run each:

```
https://www.opengraph.xyz/url/https%3A%2F%2Fkernelcad.com   ← page card
https://opengraph.xyz/url/https%3A%2F%2Fgithub.com%2Fw1ne%2FkernelCAD-web   ← repo card
```

Both should display the rendered OG image, title, and description.

- [ ] **Step 4: Mobile spot-check**

Open `https://kernelcad.com` on phone. Confirm: layout responsive, demo video plays inline (muted), buttons tappable.

- [ ] **Step 5: Final commit (if any cleanup)**

```bash
git status
git push
```

---

## Self-review checklist (run after writing this plan, before handing off)

- [ ] **Spec coverage:** every section of the spec maps to a task above (verified — Tasks 2–14 cover the landing/build, Tasks 7–12 cover brand assets, Tasks 15–16 cover deploy, Tasks 17–19 cover uploads).
- [ ] **Placeholder scan:** zero TBDs, zero "implement later"s — every step has runnable commands or actual code.
- [ ] **Type consistency:** function names match across tasks (`findLatestDemo`, `parseSemver` defined in Task 5 and not redefined elsewhere).
- [ ] **Frequent commits:** every task ends with a commit step.
- [ ] **TDD where it adds value:** Task 5 (auto-discovery) is TDD'd; rendering tasks verified visually because pixel-diff testing for design assets is over-engineering for v1.

---

## Out of plan (iteration 2+)

- `/builds` page surfacing all `docs/demos/v*/`
- Newsletter / RSS
- Dark mode for the site
- Internationalization
- Analytics (Plausible / similar)
- A separate `assets/brand/` git-tracked copy for downstream use
