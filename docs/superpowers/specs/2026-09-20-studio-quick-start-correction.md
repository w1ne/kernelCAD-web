# Quick start belongs inside Studio

User correction: keep Studio as the main product. `/` and `/studio` open the same full Studio shell. A small Quick start control inside the shell opens the three examples as new local projects, saving current edits first. Examples use Studio's param controls, viewport, history, and export tools. Source links and read-only embeds keep their supplied model.

Local CAD is open without sign-in; hosted AI keeps its existing session checks. Remove the separate start page and its duplicate viewer/state. Verify new-project preservation, full-kernel size updates, root routing, and real browser editing/export before shipping through CI and deployment.
