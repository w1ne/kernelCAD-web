# Easy start

Approved direction: the user accepted the researched example → edit → download flow and asked for simple language and fewer words.

The app root opens a small public start screen. Three real examples (phone stand, bracket, open electronics box) run in the existing browser CAD worker. Each has bounded size controls, undo, and free STL/STEP downloads. No account, generation credits, server requests, or setup are needed for these examples. Show a real model, loading state, and recoverable errors; never export a stale preview. Keep the last good model if an edit fails.

One prompt box links into the existing hosted generation flow. Clearly state that custom generation requires sign-in; do not promise a new free AI allowance. Keep the advanced Studio and existing script/gallery/headless links intact. Simplify the public homepage invitation and generation-page copy. Preserve paid generation and reference-image paths.

Verification: component behavior (edit/undo, failed build, stale results, download), real browser CAD evaluation and STL/STEP downloads, narrow screen layout, production build and lint, PR CI and deployed browser checks.
