// src/kernel/backends/verb/surfaceBridge.ts
//
// RESERVED FOR v2 (surface analytics extension, deferred per spec D6).
// Intentionally an empty stub: reserves the module path so v2 can populate
// it without churn on imports. Do NOT import this file from v1 code.

export const SURFACE_BRIDGE_RESERVED = true;
